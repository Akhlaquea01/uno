services:
  uno:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: uno-app
    ports:
      - "4000:4000"
    env_file:
      - backend/.env
    environment:
      - NODE_ENV=production
      - PORT=4000
    restart: unless-stopped
